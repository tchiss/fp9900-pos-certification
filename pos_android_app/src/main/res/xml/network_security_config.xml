<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust preinstalled CAs -->
            <certificates src="system" />
            <!-- Additionally trust user added CAs -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- Configuration spécifique pour l'API DGI -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.fiv.dgi.kpsaccess.com</domain>
        <trust-anchors>
            <!-- Trust system and user CAs -->
            <certificates src="system" />
            <certificates src="user" />
            <!-- For development/testing: allow self-signed certs if needed -->
            <!-- Remove in production if using proper CA-signed certificates -->
        </trust-anchors>
    </domain-config>
</network-security-config>
